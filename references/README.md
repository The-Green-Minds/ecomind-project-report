# References

Maintain APA 7 references and a versioned BibTeX database. Every bibliography entry must be used explicitly in the report. Verify DOI, year and journal quartile before delivery.

- `bibliography.bib`: references cited through Pandoc citeproc.
- `source-register.md`: claim-level provenance, authority and limitations.

The current academic set is not yet the complete AV1 minimum. Before delivery, verify the current SCImago/JCR quartile and add at least two recent papers about mobile techniques actually implemented, in addition to the domain papers.
